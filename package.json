{
  "name": "fangda-test-1",
  "version": "1.0.0",
  "main": "src/index.js",
  "license": "MIT",
  "scripts": {
    "dev": "rm -rf dist && parcel src/index.html --no-cache",
    "build": "rm -rf dist && parcel build src/index.html --no-minify --public-url ./"
  }
}
